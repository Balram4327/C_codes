//Program to find the closest sum to zero
#include<stdio.h>

int main()
{
    /*
    int n = 7;
    int array[] = {12,-13,-1,10,0,-34,1};

    int sum = 0;
    int diff = array[0]+array[];

    for(int i=0; i<n; i++)
    {
        for(int j=0; j<n; j++)
        {   
            sum = array[i]+array[j];
            if (sum)
        }
    }
*/

//gaaaaaaaaaaaaaaaaah no more codesssssssssssssssssssssss, that's it for now.
    return 0;
}